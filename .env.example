# Gmail Invoice Scanner - Environment Configuration

# OAuth2 Setup
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a new project or select existing one
# 3. Enable Gmail API
# 4. Create OAuth2 credentials (Web application type)
# 5. Add your domain to authorized redirect URIs (e.g., https://your-domain.com/oauth2callback)
APP_DOMAIN=your-domain.com  # Your actual domain without protocol
USE_HTTPS=true  # Always use true in production

# OpenAI Configuration
# Get your API key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Email Configuration
# Comma-separated lists of email addresses
SOURCE_EMAILS=email1@domain.com,email2@domain.com  # Emails to monitor for invoices
TARGET_EMAILS=target1@domain.com,target2@domain.com  # Emails to receive forwarded invoices

# Optional Settings
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=email_processor.log  # Path to log file

# Rate Limiting (requests per minute)
GMAIL_RATE_LIMIT=250  # Maximum Gmail API requests per minute
OPENAI_RATE_LIMIT=50  # Maximum OpenAI API requests per minute